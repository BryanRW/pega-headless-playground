# Metadata Headless Web

Placeholder for the headless application that renders UI from Pega app metadata.

Use this app for experiments where Pega metadata drives views, fields, actions, validations, and case screens while React owns the final presentation layer.
